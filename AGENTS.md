# Nuxt application

This portal uses Nuxt 4, Vue 3, Element Plus, and Nitro server routes. Keep page components in `pages/`, shared UI in `components/`, layouts in `layouts/`, and server endpoints in `server/api/`. Read the relevant Nuxt documentation before changing framework conventions or server behavior.
