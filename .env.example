# Auth.js session signing secret. Generate a strong unique value per environment.
AUTH_SECRET=
AUTH_TRUST_HOST=true

# DevConnect / basis-auth OIDC client configuration.
BASIS_AUTH_ISSUER=http://localhost:3000
BASIS_AUTH_CLIENT_ID=
BASIS_AUTH_CLIENT_SECRET=
BASIS_AUTH_RESOURCE=urn:basis:api:bisz-portal
